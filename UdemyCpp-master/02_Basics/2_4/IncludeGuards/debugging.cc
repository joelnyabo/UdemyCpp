//Debugging bedeutet Fehler im Code finden, verstehen und beheben.
// Bislang hat man nur im Debug Mode compiliert, aber Compilieren in Debug Mode ist nicht das Debugging, aber das Debugging kann man in debug modus nur machen.
//das Compilieren aus Debug Modus wird das Executable Programm erstellen, das Debugging ist dann, das wir das Compilierte von Debug Mode compilierte Programm ausführen können mit sogenannte Debugger execution
//debug -> Developement
//Release-> fertige Projekt.
// für GCC Compiler ist der GDB(DB steht für Debugger) der Debugger
// die Watch beim Debugging ist dafür gedacht dass man nur eine bestimmte Variable beobachtet.( die die uns interesiiert)

//Video 58 (Release Mode vs Debug Mode)
//je länger ein Assemblercode ist desto mehr Zeit wird der CPU brauchen zum Ausführen.
// Aus diesem Grund  ist in release der Assemblercode kürzer damit die Performance(Schnelligkeit) hocht wird,aber bei debug Mode ist es länger.
// Der Assemblercode kann man mit "Compiler Explorer" sehen.

//video59 sehr wichtig.(noch mal schauen)
